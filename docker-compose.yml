services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - AlphaVantage__ApiKey=${ALPHAVANTAGE_API_KEY}
      - ConnectionStrings__DefaultConnection=Data Source=/data/financial_tracker.db
    volumes:
      - db-data:/data

volumes:
  db-data: